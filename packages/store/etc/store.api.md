## API Report File for "@known-as-bmf/store"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import { HookSubscriber } from '@known-as-bmf/hookable';
import { TapInput } from '@known-as-bmf/hookable';
import { TapOutput } from '@known-as-bmf/hookable';
import { TransformInput } from '@known-as-bmf/hookable';

// @public
export const composeMiddlewares: <S>(
  ...middlewares: Middleware<S>[]
) => Middleware<S>;

// @public
export function deref<S>(store: Store<S>): S;

// @public
export interface Hooks<S> {
  stateDidChange: HookSubscriber<TapOutput<(state: S) => S>>;
  stateWillChange: HookSubscriber<TapInput<(state: S) => S>>;
  transformState: HookSubscriber<TransformInput<(state: S) => S>>;
}

// @public
export type Middleware<S> = (store: Store<S>, hooks: Hooks<S>) => void;

// @public
export function of<S>(initialState: S, middleware?: Middleware<S>): Store<S>;

// @public
export const pipeMiddlewares: <S>(
  ...middlewares: Middleware<S>[]
) => Middleware<S>;

// @public
export type Selector<S, R> = (state: S) => R;

// @public
export function set<S>(store: Store<S>, current: S): void;

// @public
export interface StateChangedEvent<S> {
  current: S;
  previous: S;
}

// @public
export interface Store<S> {}

// @public
export function subscribe<S>(
  store: Store<S>,
  callback: SubscriptionCallback<S>
): () => void;

// @public
export function subscribe<S, R>(
  store: Store<S>,
  callback: SubscriptionCallback<S>,
  selector: Selector<S, R>
): () => void;

// @public
export type SubscriptionCallback<S> = (event: StateChangedEvent<S>) => void;

// @public
export function swap<S>(store: Store<S>, mutationFn: (current: S) => S): void;

// (No @packageDocumentation comment for this package)
```
